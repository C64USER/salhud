db:
  image: mysql:5.6
  ports:
   - 3306:3306
  environment:
   - MYSQL_DATABASE=salhud
   - MYSQL_ROOT_PASSWORD=root
web:
  build: ./nginx
  ports:
   - 80:80
php:
  build: ./php
  links:
   - db:db
  ports:
   - 8080:80